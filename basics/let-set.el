(set 'colours '(red orange yellow green blue violet pink))
colours

; I don't have to quote the variable with setq
(setq one 1)
(setq la_lista '(1 2 3))

; multiple assign
(setq en 'english
      de 'deutsch)

; let is probably something like a local variable binding, more on that later
